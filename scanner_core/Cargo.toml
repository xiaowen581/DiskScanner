[package]
name = "scanner_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "scanner_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module", "py-clone"] }
jwalk = "0.8"

[dev-dependencies]
tempfile = "3"
